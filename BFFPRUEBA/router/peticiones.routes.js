const {Router} = require('express');
const {msPersonas, msRefecncia, msPer, msactividad} = require('../functions/peticiones.services');

const router = Router();

//router.get('/prueba1', msactividad);

module.exports = router;